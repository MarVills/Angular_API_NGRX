import { Product } from "./store/products.state";
export interface Link {
    id: number;
    name: string;
    price: string;
    imageLink: string; 
  }

export const SOLUTIONLINKS: any[] = [
  
];
